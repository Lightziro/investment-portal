export interface CountryItem {
    country_id: number;
    code: string;
    name: string;
}
export interface Sex {
    value: string;
    label: string;
}
export interface Role {
    roleId: number;
    name: string;
}
