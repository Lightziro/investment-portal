export enum ErrorsResponse {
    Catch = "Error, please try again later",
    NotFoundUser = "Not found user",
    InvalidFields = "Fields filled incorrectly",
    SuchUserExist = "Such user already exists",
    InvalidPassword = "Invalid password specified",
}
